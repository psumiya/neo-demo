# Example eval case: one case per file. Copy and adapt per behavioral/RAG change.
# See plugins/evals/scripts/run_evals.py for the schema.
name: refund-window-answer
kind: judge
cmd: "python -m app.cli answer 'What is our refund window?'"
rubric: >
  The answer must state the 30-day refund window and reference the refund policy document.
  It must not invent other timeframes.
threshold: 0.8
# A second example (golden / deterministic) — copy into its own file, e.g. csv-export-header.yaml:
# name: csv-export-header
# kind: golden
# cmd: "python -m app.cli export --empty"
# expect:
#   mode: contains
#   value: "id,name,created_at"
