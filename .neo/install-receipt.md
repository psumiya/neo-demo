# neo install receipt

Repo: `psumiya/neo-demo` · Deploy target: `` · Generated by `neo-setup`.

Files are tracked in git (revert with `git rm`). The changes below are GitHub-side state, **not**
captured in git. To fully uninstall, run `neo-setup --uninstall --dir . --repo psumiya/neo-demo`, or reverse
them by hand:

## Labels created
```
gh label delete neo:build --repo psumiya/neo-demo --yes
gh label delete agent:pr --repo psumiya/neo-demo --yes
gh label delete risk:green --repo psumiya/neo-demo --yes
gh label delete risk:yellow --repo psumiya/neo-demo --yes
gh label delete risk:red --repo psumiya/neo-demo --yes
gh label delete harness-gap --repo psumiya/neo-demo --yes
gh label delete harness-report --repo psumiya/neo-demo --yes
gh label delete incident --repo psumiya/neo-demo --yes
gh label delete rollback --repo psumiya/neo-demo --yes
```

## Auto-merge
Enabled repo-wide. Reverse: `gh repo edit psumiya/neo-demo --enable-auto-merge=false`

## Secrets
```
gh secret delete ANTHROPIC_API_KEY --repo psumiya/neo-demo

```

## Branch protection
If you ran `set-branch-protection.sh`, remove it with:
```
gh api --method DELETE repos/psumiya/neo-demo/branches/main/protection
```
