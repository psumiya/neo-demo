# Temporary: isolates which reusable-workflow ref shapes GitHub can resolve. Delete after use.
name: wf-test
on:
  workflow_dispatch:

jobs:
  metrics_direct:
    uses: psumiya/neo/.github/workflows/metrics-weekly.yml@main
    with:
      days: "7"

  factcheck_direct:
    uses: psumiya/neo/.github/workflows/claudemd-factcheck-weekly.yml@main
    secrets:
      anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}

  audit_direct:
    uses: psumiya/neo/.github/workflows/audit-weekly.yml@main
    with:
      days: "7"
